transport = "http"
url = "https://mcp.context7.com/mcp"
version = "2.0.2"
versionLockedAt = "2026-01-20"

# Optional: Set MCP_CONTEXT7_API_KEY for higher rate limits
# Service works without API key but with lower rate limits
[headers]
Authorization = "Bearer ${MCP_CONTEXT7_API_KEY}"
