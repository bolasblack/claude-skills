# MCP Grep Configuration
# GitHub code search service powered by grep.app

transport = "http"
url = "https://mcp.grep.app"
version = "0.1.0"
versionLockedAt = "2026-01-20"

# No authentication required for basic usage
# If API key support is added in the future, uncomment below:
# [headers]
# Authorization = "Bearer ${MCP_GREP_API_KEY}"
