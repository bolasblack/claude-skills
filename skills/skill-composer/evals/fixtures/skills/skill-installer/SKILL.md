---
name: skill-installer
description: "Use when the user asks to install an Agent Skill from a local package or GitHub source into an agent's skill directory. Preserve the package contents."
---

# Skill Installer

Resolve the requested source and destination, then install the package without editing it.
